<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amir Store | متجر FC موبايل الاحترافي</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a2e2a 0%, #0a1f1e 100%);
            color: #fff;
            direction: rtl;
        }

        .top-bar {
            background: #0d1f1e;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
            border-bottom: 2px solid #f9c74f;
        }

        .lang-switch button {
            background: #1e3a3a;
            border: none;
            color: white;
            padding: 8px 15px;
            margin: 0 5px;
            border-radius: 20px;
            cursor: pointer;
            font-weight: bold;
        }

        .lang-switch button.active {
            background: #f9c74f;
            color: #0a1f1e;
        }

        .city-currency-section {
            background: linear-gradient(135deg, #1e3a3a, #0d2a2a);
            border: 2px solid #f9c74f;
            border-radius: 15px;
            padding: 15px 25px;
            margin: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 15px;
        }

        .city-currency-label span {
            background: #f9c74f;
            color: #0a1f1e;
            padding: 5px 12px;
            border-radius: 30px;
            font-weight: bold;
        }

        .city-select {
            background: #f9c74f;
            color: #0a1f1e;
            border: none;
            padding: 12px 20px;
            border-radius: 40px;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            min-width: 260px;
            text-align: center;
        }

        .header {
            text-align: center;
            padding: 30px 20px;
            background: rgba(0,0,0,0.3);
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 2.5em;
            color: #f9c74f;
        }

        /* صورة الوجه لوجه - مميزة */
        .face-to-face-section {
            background: linear-gradient(135deg, #0f3a35, #0a2e2a);
            border-radius: 30px;
            margin: 20px;
            padding: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
            border: 2px solid #f9c74f;
            box-shadow: 0 0 20px rgba(249, 199, 79, 0.3);
        }

        .face-image {
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            transition: transform 0.3s;
            max-width: 280px;
            width: 100%;
            height: auto;
            border: 3px solid #f9c74f;
        }

        .face-image:hover {
            transform: scale(1.02);
        }

        .face-text {
            text-align: center;
            max-width: 400px;
        }

        .face-text h2 {
            color: #f9c74f;
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .face-text p {
            font-size: 1.1em;
            opacity: 0.9;
        }

        .section {
            margin: 40px 20px;
        }

        .section-title {
            font-size: 1.8em;
            border-right: 5px solid #f9c74f;
            padding-right: 15px;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .section-title-icon {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: #f9c74f;
            padding: 3px;
        }

        .section-title.x2 {
            border-right-color: #f94144;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
        }

        .card {
            background: rgba(30, 58, 58, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 20px;
            transition: transform 0.3s, box-shadow 0.3s;
            border: 1px solid rgba(249, 199, 79, 0.3);
            position: relative;
        }

        /* بطاقة خاصة مع أيقونة الوجه لوجه */
        .card.face-card {
            border: 2px solid #f9c74f;
            background: linear-gradient(135deg, rgba(30,58,58,0.95), rgba(15,45,40,0.95));
        }

        .card.face-card::before {
            content: "🎯";
            position: absolute;
            top: -12px;
            right: 20px;
            background: #f9c74f;
            color: #0a1f1e;
            padding: 5px 12px;
            border-radius: 30px;
            font-size: 12px;
            font-weight: bold;
        }

        .card.x2-card {
            border: 2px solid #f94144;
            background: rgba(249, 65, 68, 0.15);
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .card h3 {
            color: #f9c74f;
            margin-bottom: 15px;
        }

        .price {
            font-size: 28px;
            font-weight: bold;
            margin: 15px 0;
            color: #90e0ef;
        }

        .btn-order {
            background: #f9844a;
            width: 100%;
            border: none;
            padding: 12px;
            border-radius: 30px;
            font-weight: bold;
            font-size: 16px;
            cursor: pointer;
            transition: 0.2s;
            color: #0a1f1e;
        }

        .btn-order:hover {
            background: #f94144;
            color: white;
        }

        .badge {
            background: #f9c74f;
            color: #0a1f1e;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            display: inline-block;
            margin-bottom: 10px;
        }

        .badge.x2 {
            background: #f94144;
            color: white;
        }

        .package-badge {
            background: #f94144;
            color: white;
            padding: 3px 10px;
            border-radius: 15px;
            font-size: 11px;
            display: inline-block;
            margin-top: 5px;
        }

        .login-btn, .admin-btn {
            background: #f9c74f;
            color: #0a1f1e;
            padding: 8px 20px;
            border-radius: 30px;
            cursor: pointer;
            font-weight: bold;
            border: none;
            margin: 0 5px;
        }

        /* Admin Panel Styles */
        .admin-panel {
            background: linear-gradient(135deg, #1e3a3a, #0d2a2a);
            border-radius: 20px;
            padding: 25px;
            margin: 20px;
            border: 2px solid #f94144;
            display: none;
        }

        .admin-panel h2 {
            color: #f94144;
            margin-bottom: 20px;
            text-align: center;
        }

        .admin-tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 25px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .admin-tab {
            background: #2c5a57;
            border: none;
            padding: 12px 25px;
            border-radius: 30px;
            cursor: pointer;
            color: white;
            font-weight: bold;
            transition: 0.2s;
        }

        .admin-tab.active {
            background: #f94144;
        }

        .admin-tab:hover {
            transform: translateY(-2px);
        }

        .admin-content {
            display: none;
        }

        .admin-content.active {
            display: block;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: rgba(0,0,0,0.5);
            padding: 20px;
            border-radius: 20px;
            text-align: center;
            border: 1px solid #f9c74f;
            transition: 0.2s;
        }

        .stat-card:hover {
            transform: translateY(-3px);
            border-color: #f94144;
        }

        .stat-card h3 {
            color: #f9c74f;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .stat-card .number {
            font-size: 36px;
            font-weight: bold;
            color: #90e0ef;
        }

        .stat-card .small {
            font-size: 12px;
            opacity: 0.7;
        }

        /* Tables */
        .users-table, .orders-table, .products-table, .contacts-table {
            width: 100%;
            border-collapse: collapse;
            background: rgba(0,0,0,0.5);
            border-radius: 15px;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .users-table th, .users-table td,
        .orders-table th, .orders-table td,
        .products-table th, .products-table td,
        .contacts-table th, .contacts-table td {
            padding: 12px;
            text-align: right;
            border-bottom: 1px solid #2c5a57;
        }

        .users-table th, .orders-table th, .products-table th, .contacts-table th {
            background: #f9c74f;
            color: #0a1f1e;
        }

        .delete-btn {
            background: #f94144;
            border: none;
            padding: 5px 12px;
            border-radius: 15px;
            cursor: pointer;
            color: white;
        }

        .delete-btn:hover {
            background: #c72a2a;
        }

        /* Forms */
        .add-product-form, .add-contact-form {
            background: rgba(0,0,0,0.5);
            padding: 20px;
            border-radius: 15px;
            margin-top: 20px;
        }

        .add-product-form h4, .add-contact-form h4 {
            color: #f9c74f;
            margin-bottom: 15px;
        }

        .add-product-form input, .add-product-form select,
        .add-contact-form input, .add-contact-form textarea {
            padding: 10px;
            margin: 10px;
            border-radius: 25px;
            border: none;
            background: #2c5a57;
            color: white;
            width: calc(100% - 20px);
        }

        .add-product-form textarea, .add-contact-form textarea {
            resize: vertical;
            min-height: 80px;
        }

        .add-product-form button, .add-contact-form button {
            background: #f9c74f;
            border: none;
            padding: 10px 25px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 10px;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.95);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background: linear-gradient(135deg, #1e3a3a, #0d2a2a);
            padding: 35px;
            border-radius: 30px;
            text-align: center;
            border: 2px solid #f9c74f;
            max-width: 400px;
            width: 90%;
        }

        .modal-content h2 {
            color: #f9c74f;
            margin-bottom: 20px;
        }

        .modal-content input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border-radius: 25px;
            border: none;
            background: #2c5a57;
            color: white;
            font-size: 16px;
        }

        .modal-content button {
            width: 100%;
            padding: 12px;
            margin: 8px 0;
            border-radius: 25px;
            border: none;
            font-weight: bold;
            cursor: pointer;
        }

        .register-btn {
            background: #f9c74f;
            color: #0a1f1e;
        }

        .close-modal {
            background: #555;
            color: white;
        }

        .user-area {
            display: flex;
            align-items: center;
            gap: 15px;
            background: #1e3a3a;
            padding: 5px 15px;
            border-radius: 40px;
        }

        .logout-btn {
            background: #f94144;
            color: white;
            border: none;
            padding: 5px 12px;
            border-radius: 20px;
            cursor: pointer;
        }

        .dashboard {
            background: linear-gradient(135deg, #1e3a3a, #0d2a2a);
            border-radius: 20px;
            padding: 20px;
            margin: 20px;
            border: 1px solid #f9c74f;
            display: none;
        }

        .dashboard h3 {
            color: #f9c74f;
            margin-bottom: 15px;
        }

        footer {
            background: #0d1f1e;
            margin-top: 50px;
            padding: 30px 20px 20px;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .social-links {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .social-icon {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: #1e3a3a;
            padding: 10px 20px;
            border-radius: 40px;
            text-decoration: none;
            color: white;
            font-weight: bold;
            transition: 0.2s;
        }

        .social-icon.whatsapp { background: #25D366; }
        .social-icon.instagram { background: linear-gradient(45deg, #f09433, #d62976, #962fbf); }
        .social-icon.tiktok { background: #010101; border: 1px solid #25F4EE; }

        .social-icon:hover {
            transform: translateY(-3px);
            filter: brightness(1.05);
        }

        .copyright {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #2c4c4b;
            font-size: 14px;
            opacity: 0.7;
        }

        @media (max-width: 700px) {
            .footer-content, .city-currency-section {
                flex-direction: column;
                text-align: center;
            }
            .users-table, .orders-table, .products-table, .contacts-table {
                font-size: 12px;
            }
            .face-to-face-section {
                flex-direction: column;
                text-align: center;
            }
            .section-title {
                justify-content: center;
            }
        }

        .admin-login-area {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .export-btn {
            background: #2c5a57;
            border: none;
            padding: 8px 15px;
            border-radius: 20px;
            cursor: pointer;
            color: white;
            margin: 10px;
        }

        .export-btn:hover {
            background: #f9c74f;
            color: #0a1f1e;
        }
    </style>
</head>
<body>
    <div class="top-bar">
        <div class="lang-switch">
            <button onclick="setLanguage('ar')" id="lang-ar" class="active">🇸🇦 العربية</button>
            <button onclick="setLanguage('en')" id="lang-en">🇬🇧 English</button>
            <button onclick="setLanguage('tr')" id="lang-tr">🇹🇷 Türkçe</button>
        </div>
        <div class="admin-login-area">
            <button class="admin-btn" onclick="toggleAdminPanel()">👑 لوحة الأدمن</button>
            <div id="userStatus">
                <button class="login-btn" onclick="openModal()">🔐 تسجيل الدخول / حساب جديد</button>
            </div>
        </div>
    </div>

    <!-- نافذة تسجيل الدخول والتسجيل -->
    <div id="authModal" class="modal">
        <div class="modal-content">
            <h2 id="modalTitle">📝 إنشاء حساب جديد</h2>
            <input type="text" id="fullName" placeholder="👤 الاسم الكامل">
            <input type="tel" id="phoneNumber" placeholder="📱 رقم الهاتف">
            <input type="email" id="email" placeholder="📧 البريد الإلكتروني">
            <input type="password" id="password" placeholder="🔒 كلمة السر">
            <button id="actionBtn" class="register-btn" onclick="register()">✅ إنشاء حساب</button>
            <div class="switch-mode" onclick="toggleMode()" id="switchText" style="color:#f9c74f; cursor:pointer; margin-top:10px;">🔁 لديك حساب؟ اضغط هنا</div>
            <button class="close-modal" onclick="closeModal()">✖ إغلاق</button>
        </div>
    </div>

    <!-- نافذة تسجيل دخول الأدمن -->
    <div id="adminModal" class="modal">
        <div class="modal-content">
            <h2>👑 تسجيل دخول الأدمن</h2>
            <input type="text" id="adminUsername" placeholder="👤 اسم المستخدم">
            <input type="password" id="adminPassword" placeholder="🔒 كلمة السر">
            <button onclick="adminLogin()" class="register-btn">🔐 دخول</button>
            <button class="close-modal" onclick="closeAdminModal()">✖ إغلاق</button>
        </div>
    </div>

    <!-- لوحة المستخدم -->
    <div id="dashboard" class="dashboard">
        <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap;">
            <h3>👋 مرحباً، <span id="userName"></span>!</h3>
            <button class="logout-btn" onclick="logout()">🚪 تسجيل خروج</button>
        </div>
        <div style="margin-top: 15px;">
            <p>📧 البريد: <strong id="userEmail"></strong></p>
            <p>📱 الهاتف: <strong id="userPhone"></strong></p>
        </div>
    </div>

    <!-- لوحة تحكم الأدمن -->
    <div id="adminPanel" class="admin-panel">
        <h2>👑 لوحة تحكم الأدمن - الإحصائيات والمعلومات</h2>
        
        <div class="admin-tabs">
            <button class="admin-tab active" onclick="showAdminTab('stats')">📊 الإحصائيات</button>
            <button class="admin-tab" onclick="showAdminTab('users')">👥 المستخدمين</button>
            <button class="admin-tab" onclick="showAdminTab('orders')">📦 الطلبات</button>
            <button class="admin-tab" onclick="showAdminTab('products')">🛍️ المنتجات</button>
            <button class="admin-tab" onclick="showAdminTab('contacts')">📞 جهات الاتصال</button>
            <button class="admin-tab" onclick="showAdminTab('analytics')">📈 التحليلات</button>
        </div>

        <!-- تبويب الإحصائيات -->
        <div id="statsTab" class="admin-content active">
            <div class="stats-grid">
                <div class="stat-card">
                    <h3>👥 إجمالي المستخدمين</h3>
                    <div class="number" id="totalUsers">0</div>
                </div>
                <div class="stat-card">
                    <h3>📦 إجمالي الطلبات</h3>
                    <div class="number" id="totalOrders">0</div>
                </div>
                <div class="stat-card">
                    <h3>💰 إجمالي الإيرادات</h3>
                    <div class="number" id="totalRevenue">$0</div>
                </div>
                <div class="stat-card">
                    <h3>🛍️ عدد المنتجات</h3>
                    <div class="number" id="totalProducts">0</div>
                </div>
                <div class="stat-card">
                    <h3>📞 رسائل جهات الاتصال</h3>
                    <div class="number" id="totalContacts">0</div>
                </div>
                <div class="stat-card">
                    <h3>⭐ متوسط التقييم</h3>
                    <div class="number" id="avgRating">4.8</div>
                </div>
            </div>
            <button class="export-btn" onclick="exportData()">📥 تصدير البيانات (JSON)</button>
        </div>

        <!-- تبويب المستخدمين -->
        <div id="usersTab" class="admin-content">
            <table class="users-table">
                <thead>
                    <tr><th>الاسم</th><th>رقم الهاتف</th><th>البريد الإلكتروني</th><th>تاريخ التسجيل</th><th>إجراء</th></tr>
                </thead>
                <tbody id="usersList"></tbody>
            </table>
        </div>

        <!-- تبويب الطلبات -->
        <div id="ordersTab" class="admin-content">
            <table class="orders-table">
                <thead><tr><th>المنتج</th><th>العميل</th><th>الهاتف</th><th>السعر</th><th>التاريخ</th><th>إجراء</th></tr></thead>
                <tbody id="ordersList"></tbody>
            </table>
        </div>

        <!-- تبويب المنتجات -->
        <div id="productsTab" class="admin-content">
            <table class="products-table">
                <thead><tr><th>المنتج</th><th>السعر الأساسي</th><th>التصنيف</th><th>إجراء</th></tr></thead>
                <tbody id="productsList"></tbody>
            </table>
            <div class="add-product-form">
                <h4>➕ إضافة منتج جديد</h4>
                <input type="text" id="newProductName" placeholder="اسم المنتج">
                <input type="number" id="newProductPrice" placeholder="السعر">
                <select id="newProductCategory">
                    <option value="تصنيف">رفع تصنيف</option>
                    <option value="خدمات">خدمات منفردة</option>
                    <option value="TOTS">عروض TOTS</option>
                    <option value="FC">FC Points</option>
                </select>
                <button onclick="addProduct()">➕ إضافة المنتج</button>
            </div>
        </div>

        <!-- تبويب جهات الاتصال -->
        <div id="contactsTab" class="admin-content">
            <table class="contacts-table">
                <thead><tr><th>الاسم</th><th>البريد</th><th>الرسالة</th><th>التاريخ</th><th>إجراء</th></tr></thead>
                <tbody id="contactsList"></tbody>
            </table>
            <div class="add-contact-form">
                <h4>📞 إضافة جهة اتصال</h4>
                <input type="text" id="contactName" placeholder="الاسم">
                <input type="email" id="contactEmail" placeholder="البريد الإلكتروني">
                <textarea id="contactMessage" placeholder="الرسالة"></textarea>
                <button onclick="addContact()">💾 حفظ جهة الاتصال</button>
            </div>
        </div>

        <!-- تبويب التحليلات -->
        <div id="analyticsTab" class="admin-content">
            <div class="stats-grid">
                <div class="stat-card">
                    <h3>📊 أكثر منتج مبيعاً</h3>
                    <div class="number" id="topProduct">-</div>
                </div>
                <div class="stat-card">
                    <h3>👤 أكثر مستخدم نشاطاً</h3>
                    <div class="number" id="topUser">-</div>
                </div>
                <div class="stat-card">
                    <h3>📅 طلبات آخر 7 أيام</h3>
                    <div class="number" id="weeklyOrders">0</div>
                </div>
                <div class="stat-card">
                    <h3>💰 متوسط قيمة الطلب</h3>
                    <div class="number" id="avgOrderValue">$0</div>
                </div>
            </div>
            <button class="export-btn" onclick="resetAllData()" style="background:#f94144;">🗑️ إعادة ضبط جميع البيانات</button>
        </div>
    </div>

    <div class="city-currency-section">
        <div class="city-currency-label">
            💵 <span>اختر عملتك</span>
        </div>
        <select id="city-select" class="city-select" onchange="changeCity()">
            <option value="USD">🇺🇸 دولار أمريكي - USD ($)</option>
            <option value="EUR">🇪🇺 يورو - EUR (€)</option>
            <option value="TRY">🇹🇷 ليرة تركية - TRY (₺)</option>
            <option value="SYP">🇸🇾 ليرة سورية - SYP (ل.س)</option>
            <option value="IQD">🇮🇶 دينار عراقي - IQD (د.ع)</option>
            <option value="SAR">🇸🇦 ريال سعودي - SAR (﷼)</option>
            <option value="AED">🇦🇪 درهم إماراتي - AED (د.إ)</option>
        </select>
        <div style="font-size:13px; opacity:0.8;">✨ الأسعار أدناه ستظهر بالعملة التي تختارها</div>
    </div>

    <div class="header">
        <h1>⚡ AMIR STORE ⚡</h1>
        <p id="slogan">أسرع خدمات رفع التصنيف والشحن | دعم 24 ساعة</p>
    </div>

    <!-- قسم صورة الوجه لوجه المميز -->
    <div class="face-to-face-section">
        <img src="https://i.ibb.co/G4XpsNNj/Face-to-face-jpg.jpg" alt="خدمة الوجه لوجه - رفع التصنيف" class="face-image">
        <div class="face-text">
            <h2>🎯 رفع التصنيف يدويًا - وجه لوجه</h2>
            <p>خدمة احترافية آمنة 100%، رفع تصنيف يدوي حتى 50 نجمة<br>
            🔥 ضمان الجودة | 🔐 خصوصية تامة | ⚡ تنفيذ سريع</p>
        </div>
    </div>

    <!-- قسم المنتجات -->
    <div class="section">
        <div class="section-title">
            <img src="https://i.ibb.co/G4XpsNNj/Face-to-face-jpg.jpg" class="section-title-icon" alt="icon">
            🌟 رفع تصنيف إلى 50 نجمة
        </div>
        <div class="products-grid" id="productsGrid1"></div>
    </div>

    <div class="section">
        <div class="section-title">⚔️ خدمات منفردة</div>
        <div class="products-grid" id="productsGrid2"></div>
    </div>

    <div class="section">
        <div class="section-title">🌟 عروض خاصة - TOTS</div>
        <div class="products-grid" id="productsGrid3"></div>
    </div>

    <div class="section">
        <div class="section-title x2">💥 FC POINTS X2</div>
        <div class="products-grid" id="productsGrid4"></div>
    </div>

    <!-- نموذج جهة اتصال في التذييل -->
    <div class="section" style="background: rgba(0,0,0,0.3); padding: 20px; border-radius: 20px; margin: 20px;">
        <div class="section-title">📞 تواصل معنا</div>
        <div class="add-contact-form" style="background: transparent;">
            <input type="text" id="footerContactName" placeholder="👤 الاسم الكامل">
            <input type="email" id="footerContactEmail" placeholder="📧 البريد الإلكتروني">
            <textarea id="footerContactMessage" placeholder="✏️ رسالتك"></textarea>
            <button onclick="submitContactFromFooter()">📨 إرسال</button>
        </div>
    </div>

    <footer>
        <div class="footer-content">
            <div class="social-links">
                <a href="https://wa.me/905356387200" target="_blank" class="social-icon whatsapp">💬 واتساب</a>
                <a href="https://www.instagram.com/amir_gaming820" target="_blank" class="social-icon instagram">📷 انستغرام</a>
                <a href="https://www.tiktok.com/@amir_gaming820" target="_blank" class="social-icon tiktok">🎵 تيك توك</a>
            </div>
            <div>💎 الدعم الفني 24 ساعة</div>
        </div>
        <div class="copyright">© 2026 Amir Store - جميع الخدمات يدوية وآمنة</div>
    </footer>

    <script>
        // ========== البيانات الأساسية ==========
        let users = JSON.parse(localStorage.getItem('amir_users')) || [];
        let orders = JSON.parse(localStorage.getItem('amir_orders')) || [];
        let contacts = JSON.parse(localStorage.getItem('amir_contacts')) || [];
        let products = JSON.parse(localStorage.getItem('amir_products')) || [
            { id: 1, name: "من 0 إلى 50 نجمة", price: 30, category: "تصنيف" },
            { id: 2, name: "VS Mode", price: 30, category: "خدمات" },
            { id: 3, name: "Competitive Attack", price: 30, category: "خدمات" },
            { id: 4, name: "Manager", price: 30, category: "خدمات" },
            { id: 5, name: "الحزمة الكاملة", price: 75, category: "خدمات" },
            { id: 6, name: "TOTS RIQUELME", price: 12, category: "TOTS" },
            { id: 7, name: "700 Upgrade", price: 6, category: "TOTS" },
            { id: 8, name: "1000 FC X2", price: 6, category: "FC" },
            { id: 9, name: "2000 FC X2", price: 12, category: "FC" },
            { id: 10, name: "4000 FC X2", price: 24, category: "FC" }
        ];
        let nextProductId = 11;

        let currentLang = 'ar';
        let currentCurrency = 'USD';
        let isLoginMode = false;
        let isAdminLoggedIn = false;

        const currencyRates = {
            USD: { symbol: '$', rate: 1 },
            EUR: { symbol: '€', rate: 0.92 },
            TRY: { symbol: '₺', rate: 39.58 },
            SYP: { symbol: 'ل.س', rate: 135 },
            IQD: { symbol: 'د.ع', rate: 1416.67 },
            SAR: { symbol: '﷼', rate: 3.33 },
            AED: { symbol: 'د.إ', rate: 3.33 }
        };

        // حفظ البيانات
        function saveData() {
            localStorage.setItem('amir_users', JSON.stringify(users));
            localStorage.setItem('amir_orders', JSON.stringify(orders));
            localStorage.setItem('amir_contacts', JSON.stringify(contacts));
            localStorage.setItem('amir_products', JSON.stringify(products));
        }

        // عرض المنتجات
        function displayProducts() {
            const categories = {
                تصنيف: 'productsGrid1',
                خدمات: 'productsGrid2',
                TOTS: 'productsGrid3',
                FC: 'productsGrid4'
            };
            
            for (let cat in categories) {
                const grid = document.getElementById(categories[cat]);
                if (grid) {
                    const catProducts = products.filter(p => p.category === cat);
                    grid.innerHTML = catProducts.map(p => `
                        <div class="card ${p.name.includes('50 نجمة') ? 'face-card' : ''} ${p.category === 'FC' ? 'x2-card' : ''}">
                            <div class="badge ${p.category === 'FC' ? 'x2' : ''}">${p.category === 'FC' ? 'X2' : (p.category === 'تصنيف' ? '🎯 يدوي 100%' : '⭐')}</div>
                            <h3>${p.name.includes('50 نجمة') ? '🎯 ' + p.name : p.name}</h3>
                            <div class="price" data-base="${p.price}">${currencyRates[currentCurrency].symbol} ${(p.price * currencyRates[currentCurrency].rate).toFixed(2)}</div>
                            <button class="btn-order" onclick="orderNow('${p.name}', ${p.price})">📦 اطلب الخدمة</button>
                        </div>
                    `).join('');
                }
            }
            updateAllPrices();
        }

        // طلب المنتج
        function orderNow(product, price) {
            const savedUser = localStorage.getItem('amir_user');
            if (!savedUser) {
                alert('⚠️ الرجاء تسجيل الدخول أولاً');
                openModal();
                return;
            }
            const user = JSON.parse(savedUser);
            
            // حفظ الطلب
            const newOrder = {
                id: Date.now(),
                product: product,
                customer: user.name,
                phone: user.phone,
                price: price,
                date: new Date().toLocaleString('ar-EG')
            };
            orders.push(newOrder);
            saveData();
            
            const currency = document.getElementById('city-select').options[document.getElementById('city-select').selectedIndex].text;
            const msg = `مرحباً، أريد طلب: ${product}\n👤 الاسم: ${user.name}\n📱 الهاتف: ${user.phone}\n📧 البريد: ${user.email}\n💰 العملة: ${currency}`;
            window.open(`https://wa.me/905356387200?text=${encodeURIComponent(msg)}`, '_blank');
            
            if (isAdminLoggedIn) {
                refreshAdminTables();
            }
        }

        // تسجيل مستخدم جديد
        function register() {
            const name = document.getElementById('fullName').value.trim();
            const phone = document.getElementById('phoneNumber').value.trim();
            const email = document.getElementById('email').value.trim();
            const password = document.getElementById('password').value.trim();

            if (!name || !phone || !email || !password) {
                alert('❌ الرجاء ملء جميع الحقول');
                return;
            }

            const userData = { name, phone, email, password, registerDate: new Date().toLocaleString('ar-EG') };
            users.push(userData);
            saveData();
            
            localStorage.setItem('amir_user', JSON.stringify(userData));
            showDashboard(userData);
            closeModal();
            alert('✅ تم إنشاء الحساب بنجاح!');
            if (isAdminLoggedIn) refreshAdminTables();
        }

        // تسجيل الدخول
        function login() {
            const email = document.getElementById('email').value.trim();
            const password = document.getElementById('password').value.trim();

            const user = users.find(u => u.email === email && u.password === password);
            if (user) {
                showDashboard(user);
                closeModal();
                alert('✅ تم تسجيل الدخول بنجاح!');
            } else {
                alert('❌ البريد الإلكتروني أو كلمة السر غير صحيحة');
            }
        }

        // عرض لوحة المستخدم
        function showDashboard(user) {
            document.getElementById('dashboard').style.display = 'block';
            document.getElementById('userName').innerText = user.name;
            document.getElementById('userEmail').innerText = user.email;
            document.getElementById('userPhone').innerText = user.phone;
            localStorage.setItem('amir_user', JSON.stringify(user));
            updateUserStatus(user.name);
        }

        function updateUserStatus(name) {
            document.getElementById('userStatus').innerHTML = `
                <div class="user-area">
                    <span>👤 ${name}</span>
                    <button class="logout-btn" onclick="logout()">خروج</button>
                </div>
            `;
        }

        function logout() {
            localStorage.removeItem('amir_user');
            document.getElementById('dashboard').style.display = 'none';
            document.getElementById('userStatus').innerHTML = `<button class="login-btn" onclick="openModal()">🔐 تسجيل الدخول / حساب جديد</button>`;
        }

        // ========== وظائف الأدمن ==========
        function toggleAdminPanel() {
            if (!isAdminLoggedIn) {
                document.getElementById('adminModal').style.display = 'flex';
            } else {
                document.getElementById('adminPanel').style.display = 'none';
                isAdminLoggedIn = false;
            }
        }

        function closeAdminModal() {
            document.getElementById('adminModal').style.display = 'none';
        }

        function adminLogin() {
            const username = document.getElementById('adminUsername').value.trim();
            const password = document.getElementById('adminPassword').value.trim();
            
            if (username === 'admin' && password === 'admin123') {
                isAdminLoggedIn = true;
                document.getElementById('adminPanel').style.display = 'block';
                closeAdminModal();
                refreshAdminTables();
                alert('✅ مرحباً أدمن!');
            } else {
                alert('❌ اسم المستخدم أو كلمة السر غير صحيحة');
            }
        }

        function showAdminTab(tab) {
            document.querySelectorAll('.admin-content').forEach(t => t.classList.remove('active'));
            document.getElementById(`${tab}Tab`).classList.add('active');
            document.querySelectorAll('.admin-tab').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            if (tab === 'stats' || tab === 'analytics') refreshStats();
            if (tab === 'users') refreshUsersTable();
            if (tab === 'orders') refreshOrdersTable();
            if (tab === 'products') refreshProductsTable();
            if (tab === 'contacts') refreshContactsTable();
        }

        function refreshStats() {
            const totalRevenue = orders.reduce((sum, o) => sum + o.price, 0);
            document.getElementById('totalUsers').innerText = users.length;
            document.getElementById('totalOrders').innerText = orders.length;
            document.getElementById('totalRevenue').innerText = `$${totalRevenue}`;
            document.getElementById('totalProducts').innerText = products.length;
            document.getElementById('totalContacts').innerText = contacts.length;
        }

        function refreshUsersTable() {
            const tbody = document.getElementById('usersList');
            tbody.innerHTML = users.map((u, i) => `
                <tr>
                    <td>${u.name}</td>
                    <td>${u.phone}</td>
                    <td>${u.email}</td>
                    <td>${u.registerDate || 'غير محدد'}</td>
                    <td><button class="delete-btn" onclick="deleteUser(${i})">🗑️ حذف</button></td>
                </tr>
            `).join('');
        }

        function refreshOrdersTable() {
            const tbody = document.getElementById('ordersList');
            tbody.innerHTML = orders.map((o, i) => `
                <tr>
                    <td>${o.product}</td>
                    <td>${o.customer}</td>
                    <td>${o.phone}</td>
                    <td>$${o.price}</td>
                    <td>${o.date}</td>
                    <td><button class="delete-btn" onclick="deleteOrder(${i})">🗑️ حذف</button></td>
                </tr>
            `).join('');
        }

        function refreshProductsTable() {
            const tbody = document.getElementById('productsList');
            tbody.innerHTML = products.map((p, i) => `
                <tr>
                    <td>${p.name}</td>
                    <td>$${p.price}</td>
                    <td>${p.category}</td>
                    <td><button class="delete-btn" onclick="deleteProduct(${i})">🗑️ حذف</button></td>
                </tr>
            `).join('');
        }

        function refreshContactsTable() {
            const tbody = document.getElementById('contactsList');
            tbody.innerHTML = contacts.map((c, i) => `
                <tr>
                    <td>${c.name}</td>
                    <td>${c.email}</td>
                    <td>${c.message.substring(0, 50)}...</td>
                    <td>${c.date}</td>
                    <td><button class="delete-btn" onclick="deleteContact(${i})">🗑️ حذف</button></td>
                </tr>
            `).join('');
        }

        function refreshAdminTables() {
            refreshStats();
            refreshUsersTable();
            refreshOrdersTable();
            refreshProductsTable();
            refreshContactsTable();
            
            // تحليلات إضافية
            if (orders.length > 0) {
                const productCounts = {};
                orders.forEach(o => productCounts[o.product] = (productCounts[o.product] || 0) + 1);
                const topProduct = Object.entries(productCounts).sort((a,b) => b[1] - a[1])[0];
                document.getElementById('topProduct').innerText = topProduct ? topProduct[0] : '-';
                
                const avgValue = orders.reduce((s,o) => s + o.price, 0) / orders.length;
                document.getElementById('avgOrderValue').innerText = `$${avgValue.toFixed(2)}`;
                
                const last7Days = orders.filter(o => {
                    const orderDate = new Date(o.date);
                    const weekAgo = new Date();
                    weekAgo.setDate(weekAgo.getDate() - 7);
                    return orderDate > weekAgo;
                }).length;
                document.getElementById('weeklyOrders').innerText = last7Days;
            }
        }

        function deleteUser(index) { users.splice(index, 1); saveData(); refreshUsersTable(); refreshStats(); }
        function deleteOrder(index) { orders.splice(index, 1); saveData(); refreshOrdersTable(); refreshStats(); }
        function deleteProduct(index) { products.splice(index, 1); saveData(); refreshProductsTable(); refreshStats(); displayProducts(); }
        function deleteContact(index) { contacts.splice(index, 1); saveData(); refreshContactsTable(); refreshStats(); }

        function addProduct() {
            const name = document.getElementById('newProductName').value.trim();
            const price = parseFloat(document.getElementById('newProductPrice').value);
            const category = document.getElementById('newProductCategory').value;
            if (!name || isNaN(price)) { alert('❌ الرجاء إدخال بيانات صحيحة'); return; }
            products.push({ id: nextProductId++, name, price, category });
            saveData();
            refreshProductsTable();
            refreshStats();
            displayProducts();
            document.getElementById('newProductName').value = '';
            document.getElementById('newProductPrice').value = '';
            alert('✅ تم إضافة المنتج بنجاح!');
        }

        function addContact() {
            const name = document.getElementById('contactName').value.trim();
            const email = document.getElementById('contactEmail').value.trim();
            const message = document.getElementById('contactMessage').value.trim();
            if (!name || !email || !message) { alert('❌ الرجاء ملء جميع الحقول'); return; }
            contacts.push({ name, email, message, date: new Date().toLocaleString('ar-EG') });
            saveData();
            refreshContactsTable();
            refreshStats();
            document.getElementById('contactName').value = '';
            document.getElementById('contactEmail').value = '';
            document.getElementById('contactMessage').value = '';
            alert('✅ تم إضافة جهة الاتصال!');
        }

        function submitContactFromFooter() {
            const name = document.getElementById('footerContactName').value.trim();
            const email = document.getElementById('footerContactEmail').value.trim();
            const message = document.getElementById('footerContactMessage').value.trim();
            if (!name || !email || !message) { alert('❌ الرجاء ملء جميع الحقول'); return; }
            contacts.push({ name, email, message, date: new Date().toLocaleString('ar-EG') });
            saveData();
            document.getElementById('footerContactName').value = '';
            document.getElementById('footerContactEmail').value = '';
            document.getElementById('footerContactMessage').value = '';
            alert('✅ تم إرسال رسالتك! سنتواصل معك قريباً');
            if (isAdminLoggedIn) refreshContactsTable();
        }

        function exportData() {
            const allData = { users, orders, contacts, products };
            const dataStr = JSON.stringify(allData, null, 2);
            const blob = new Blob([dataStr], {type: 'application/json'});
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = `amir_store_data_${new Date().toISOString()}.json`;
            a.click();
            URL.revokeObjectURL(url);
        }

        function resetAllData() {
            if (confirm('⚠️ تحذير: هذا سيحذف جميع البيانات! هل أنت متأكد؟')) {
                localStorage.clear();
                users = [];
                orders = [];
                contacts = [];
                products = [
                    { id: 1, name: "من 0 إلى 50 نجمة", price: 30, category: "تصنيف" },
                    { id: 2, name: "VS Mode", price: 30, category: "خدمات" },
                    { id: 3, name: "Competitive Attack", price: 30, category: "خدمات" },
                    { id: 4, name: "Manager", price: 30, category: "خدمات" },
                    { id: 5, name: "الحزمة الكاملة", price: 75, category: "خدمات" },
                    { id: 6, name: "TOTS RIQUELME", price: 12, category: "TOTS" },
                    { id: 7, name: "700 Upgrade", price: 6, category: "TOTS" },
                    { id: 8, name: "1000 FC X2", price: 6, category: "FC" },
                    { id: 9, name: "2000 FC X2", price: 12, category: "FC" },
                    { id: 10, name: "4000 FC X2", price: 24, category: "FC" }
                ];
                nextProductId = 11;
                saveData();
                refreshAdminTables();
                displayProducts();
                alert('✅ تم إعادة ضبط جميع البيانات!');
            }
        }

        // ========== وظائف عامة ==========
        function openModal() { document.getElementById('authModal').style.display = 'flex'; resetToRegister(); }
        function closeModal() { document.getElementById('authModal').style.display = 'none'; }
        function toggleMode() { isLoginMode ? resetToRegister() : setToLogin(); }
        function resetToRegister() { isLoginMode = false; document.getElementById('modalTitle').innerText = '📝 إنشاء حساب جديد'; document.getElementById('actionBtn').innerText = '✅ إنشاء حساب'; document.getElementById('actionBtn').onclick = register; document.getElementById('switchText').innerText = '🔁 لديك حساب؟ اضغط هنا'; document.getElementById('fullName').style.display = 'block'; document.getElementById('phoneNumber').style.display = 'block'; }
        function setToLogin() { isLoginMode = true; document.getElementById('modalTitle').innerText = '🔐 تسجيل الدخول'; document.getElementById('actionBtn').innerText = '🔑 دخول'; document.getElementById('actionBtn').onclick = login; document.getElementById('switchText').innerText = '🔁 ليس لديك حساب؟ اضغط هنا'; document.getElementById('fullName').style.display = 'none'; document.getElementById('phoneNumber').style.display = 'none'; }

        function changeCity() { currentCurrency = document.getElementById('city-select').value; updateAllPrices(); }
        function updateAllPrices() { const rate = currencyRates[currentCurrency].rate; const symbol = currencyRates[currentCurrency].symbol; document.querySelectorAll('.price').forEach(el => { let base = parseFloat(el.getAttribute('data-base')) || 0; el.innerHTML = `${symbol} ${(base * rate).toFixed(2)}`; }); }

        function setLanguage(lang) { currentLang = lang; document.body.style.direction = lang === 'ar' ? 'rtl' : 'ltr'; document.querySelectorAll('.lang-switch button').forEach(btn => btn.classList.remove('active')); document.getElementById(`lang-${lang}`).classList.add('active'); const texts = { ar: { slogan: "أسرع خدمات رفع التصنيف والشحن | دعم 24 ساعة" }, en: { slogan: "Fastest ranking & top-up services | 24/7 Support" }, tr: { slogan: "En hızlı sıralama ve yükleme hizmetleri | 7/24 Destek" } }; document.getElementById('slogan').innerText = texts[lang].slogan; }

        function checkSession() { const savedUser = localStorage.getItem('amir_user'); if (savedUser) { const user = JSON.parse(savedUser); showDashboard(user); } }

        document.addEventListener('keypress', function(e) { if (e.key === 'Enter' && document.getElementById('authModal').style.display === 'flex') { e.preventDefault(); isLoginMode ? login() : register(); } });

        // تهيئة الصفحة
        checkSession();
        displayProducts();
        updateAllPrices();
        setLanguage('ar');
    </script>
</body>
</html>